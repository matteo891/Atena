---
id: CHG-2026-05-02-042
date: 2026-05-02
adr_ref: ADR-0009, ADR-0011, ADR-0017, ADR-0018
commit: TBD
---

## What

Revert dei 3 CHG generati dopo il confronto col file Leader
`ordine_scaler500k (22).xlsx`:

- **CHG-2026-05-02-039** (`4813222`) — calibration toolkit ground truth
  ScalerBot500K (golden test 7 ASIN + fixture inline).
- **CHG-2026-05-02-040** (`5bfa844`) — errata ADR-0017 fee_fba atomica
  Keepa (decisione α'' invertita: sostituiva L11b con
  `pickAndPackFee` Keepa quando disponibile).
- **CHG-2026-05-02-041** (`4717755`) — hotfix proiezione compound r-cap
  conservativo (clamp r in [veto, 15%]).

+ i 2 commit di backfill hash associati a CHG-039 e CHG-040.

| Operazione | Cosa |
|---|---|
| `git revert --no-commit 4813222..4717755` | annulla i 5 commit (3 feat + 2 backfill) come modifiche al working tree, senza creare 5 revert commits separati. |
| Singolo commit unificato CHG-042 | applica le modifiche di revert in un'unica voce ADR-trackable. |
| Codice ripristinato | identico a HEAD pre-CHG-039 (`c689799`). |
| Test count | torna a 1036 (era 1052 post-CHG-041). |

## Why

Decisione Leader 2026-05-02 round 7+ post-CHG-041: revertare le
modifiche generate dopo il confronto col file ground truth Leader.
Il Leader ha esplicitato cambio di idea ("mi sono sbagliato possiamo
revertare le modifiche a prima che ti facessi confrontare col file?").

Implicazioni operative:
- TALOS torna a usare `fee_fba_manual` L11b verbatim (Frozen, ~€22
  Samsung). Decisione α'' originale (CHG-2026-05-01-015) ripristinata.
- ADR-0017 sezione `## Errata` rimossa (la decisione invertita non è
  più ratificata).
- Pipeline propagation Keepa→ResolutionCandidate→ResolvedRow per il
  campo `fee_fba_eur` rimossa.
- Proiezione compound torna alla formula `(1+r_actual)^N` senza cap
  (può produrre proiezioni esplosive su cart con r alto).
- Golden test ground truth ScalerBot500K rimosso. Il file Leader
  resta sul disco (non versionato).

ADR-0011 compliance: nessun force-push, storia git preservata.
ADR-0009 errata mechanism: il revert NON è un'errata corrige
classica (non modifica un ADR Active), è un rollback di codice
applicativo. Compatibile con ADR-0011 push policy single-branch.

## Tests

ruff/format/mypy strict OK. **894 PASS** (876 unit/gov/golden + 18
integration), 120 skipped, 13 errors (env var DB bootstrap mancanti
— pre-esistenti, non causati dal revert).

Test rimossi:
- `tests/golden/test_ground_truth_scaler500k.py` (CHG-039) — 7 test.
- `tests/unit/test_orchestrator_fee_fba_keepa_priority.py` (CHG-040) —
  3 test.
- 3 test in `test_keepa_client.py` post-CHG-040 (atomica field /
  fetch_fee_fba atomica / KeepaMiss).
- 3 test in `test_dashboard_cycle_overview.py` post-CHG-041 (cap
  scenari).

Tot: 16 test rimossi.

## Test di Conformità

- ADR-0009: il revert NON è errata corrige (non tocca ADR Active).
- ADR-0011: no force-push, single-branch policy preservata.
- ADR-0017 ripristinato a versione pre-CHG-040 (decisione α''
  originale: `_LiveKeepaAdapter` ritorna sempre `fee_fba_eur=None`).
- R-01 NO SILENT DROPS: il rollback è esplicito + documentato in CHG-042.

## Refs

- ADR-0009, ADR-0011.
- Commits revertati: `4813222`, `39b261a`, `5bfa844`, `bf75142`,
  `4717755`.
- Pre-revert HEAD: `4717755`. Post-revert HEAD atteso: nuovo commit
  CHG-042 che riporta il working tree allo stato di `c689799`.
- File ground truth Leader (`ordine_scaler500k (22).xlsx`) NON
  versionato — resta su disco utente, ricaricabile in futuro se
  cambia idea.
- Commit: TBD.
